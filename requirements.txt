# Bu repo — Python ortamı
#
# İzole venv: `.venv/` (repoda, .gitignore'da). Oluşturmak için:
#     python3 -m venv .venv && .venv/bin/pip install -r requirements.lock.txt
#
# `requirements.lock.txt` = canlı repo (`../enerji_fiyat_tahmini/.venv`) ile
# BİREBİR aynı 146 paket (`pip freeze`). 28 Ağu 2026'da senkron edildi.
# Feature-parity doğrulandı: vendor edilmiş `src/features/feature_engineering.py`
# canlı modelle byte-identik çıktı üretiyor (2208 saat × 70 özellik, max|Δ|=0).
#
# Canlı ile senkron kalmak için (canlı .venv güncellenince):
#     ../enerji_fiyat_tahmini/.venv/bin/pip freeze > requirements.lock.txt
#     .venv/bin/pip install -r requirements.lock.txt
#
# Aşağıdaki liste okunabilirlik için; kesin sürümler lock dosyasında.

# --- Ortak DB + veri (canlı repo ile aynı) ---
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
lightgbm==4.7.0
joblib==1.5.3
eptr2==1.3.8            # EPİAŞ Şeffaflık — 2019-2020 tarihsel çekim
requests==2.34.2
httpx==0.28.1

# --- Deney/araştırma (canlı repoda yok ama .venv'de var) ---
torch==2.13.0
xgboost==3.2.0
shap==0.51.0
statsmodels==0.15.0     # yapısal kırılma testi (C.5.0), ARIMA/ETS (Faz E) — canlı .venv'de YOK, buraya ekli
matplotlib==3.11.1
seaborn==0.13.2
yfinance==1.5.2         # Brent tarihsel
pypdf==6.16.1           # literatür PDF okuma

# --- Notebook ---
jupyter==1.1.1
nbconvert==7.17.1
nbformat==5.10.4
ipykernel==7.3.0

# --- Haber arşivi (kriz hattı) ---
beautifulsoup4==4.15.0
lxml==6.1.1

# NOT: `epftoolbox` (gerçek referans LEAR/DNN) BURAYA GİRMEZ — numpy<2 +
# tensorflow zorunluluğu. Ayrı izole venv'de: bkz. memory `epftoolbox-venv`,
# `experiments/notebooks/07_lago_protocol/run_lago_lear_reference.py`.
#
# NOT: `statsmodels` canlı .venv'de yok, bu repoda EKLİ (yukarıda). Canlı ile
# senkron ederken `pip install -r requirements.lock.txt` kullan (lock'ta var).
