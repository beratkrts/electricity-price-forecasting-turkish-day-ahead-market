# Ortak DB erişimi (canlı repo ile aynı sürümler)
SQLAlchemy==2.0.28
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.1.post1
lightgbm==4.3.0
scipy==1.12.0
joblib==1.3.2

# Deney/araştırmaya özel — canlı repoda YOK
torch==2.2.1
matplotlib==3.8.3
nbformat==5.10.2
shap==0.45.0
tqdm==4.66.2

# Haber arşivi (kriz hattı)
beautifulsoup4==4.15.0
lxml==6.1.1

# HTTP
requests==2.31.0
httpx==0.27.0

# NOT: `epftoolbox` (gerçek referans LEAR/DNN kütüphanesi) buraya eklenmedi —
# numpy<2 zorunluluğu ve tensorflow/keras/hyperopt bağımlılığı yüzünden ayrı,
# izole bir venv'de tutuluyor (bkz. experiments/notebooks/07_lago_protocol/).
